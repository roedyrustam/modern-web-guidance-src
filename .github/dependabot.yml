# Noise reduction strategy:
# 1. Weekly updates on Sundays to avoid daily interruptions.
# 2. Group ALL updates (major, minor, patch) into a single PR per project.
# 3. Limit total open PRs to prevent backlog accumulation.

version: 2
updates:
  - package-ecosystem: "npm"
    directories:
      - "/"
      - "/serving"
      - "/harness"
      - "/harness/base_apps/devtools-times"
      - "/guides"
      - "/eval-view"
    schedule:
      interval: "weekly"
      day: "sunday"
    open-pull-requests-limit: 3
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
    allow:
      - dependency-name: "web-features"
      - dependency-name: "*"
    cooldown:
      default-days: 14
    groups:
      everything:
        patterns:
          - "*"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 1
    cooldown:
      default-days: 14
