---
base_app: daily-grind
---
- restructure the seasonal favorites section into an interactive card slider using Next and Previous navigation buttons (IDs: 'next' and 'prev'). cycle through the cards using the CSS View Transitions API to apply a smooth, quick directional slide transition (next slides left, previous slides right). ensure animations are completely disabled under prefers-reduced-motion.
- make the seasonal favorites section on the homepage interactive by converting it to a slider that displays one card at a time with buttons to navigate back and forth. implement a smooth directional sliding animation using the CSS View Transitions API. respect prefers-reduced-motion by disabling animations for users who prefer reduced motion.
- implement a horizontal carousel slider for the seasonal favorites section with Next and Previous buttons (IDs: 'next' and 'prev'). trigger fluid sliding transitions using the CSS View Transitions API with types 'forward' and 'backward'. make sure to completely disable all transition animations when prefers-reduced-motion is active.
