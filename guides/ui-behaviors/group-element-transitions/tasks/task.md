---
base_app: daily-grind
---
- add a list of coffee bean selections to the daily grind homepage. start with 3 list items styled with the class 'item', and add an 'Add Item' button to dynamically add new custom beans to the list. when elements are added or removed, make them animate smoothly. specifically, the transition should apply to all items in the list using a shared transition class to define the transition with an ease-in-out timing function of 0.5s duration. each item should have its own unique transition name/identity so the browser can track it. new items entering should slide in from the left over 1 second, and exiting items should slide out to the left over 1 second. disable any full-page transition effects so that the 'Add Item' button remains clickable and responsive during the transition. also, make sure to disable all animations if the user has a prefers-reduced-motion preference set.
- can you add an interactive coffee order builder section below the seasonal favorites? i want to be able to add new custom drink items to the list by clicking an 'Add Item' button, and each card should fade in or slide when added, while keeping other items animating smoothly. make sure the animations don't lock up the UI so the user can still spam the add button, and turn them off if prefers-reduced-motion is active.
