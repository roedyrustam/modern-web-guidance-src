---
base_app: daily-grind
---
- can you add a photo carousel showcasing our cafe vibe right above the seasonal favorites section on the homepage? make a horizontal scrolling container with id 'gallery' and some 'gallery-item' divs inside it. also add a row of `<button>` elements with class 'thumbnail' for each item. i want real-time visual feedback: while the user is mid-scroll, the thumbnail that corresponds to the item they are swiping towards should immediately get the class 'pending' so they know where it's snapping. once the scroll finishes, clean up the 'pending' class, add an 'active' class, and set aria-current to true on the selected thumbnail.
- add a beautiful scrolling coffee slideshow to the homepage with a row of thumbnail indicator buttons underneath. the thumbnail buttons should have class 'thumbnail', the scroll container should have id 'gallery', and each slide should have class 'gallery-item'. make the indicators react dynamically in real-time to the scroll position, showing a preview highlight of the next slide during active scrolling before it settles.
- implement a horizontal photo viewer for our seasonal coffee menu. i need a main scroll-container with the id 'gallery' and child elements with the class 'gallery-item'. add a list of `<button>` thumbnails of class 'thumbnail' that correspond to each slide. the thumbnail of the slide the user is dragging towards needs to instantly get highlighted with the 'pending' class, and once the scrolling stops, the 'pending' class should go away and the final thumbnail should get 'active' and aria-current='true'.
