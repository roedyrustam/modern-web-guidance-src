---
base_app: daily-grind
---
- replace the logout link in the nav with a button that opens a confirmation dialog. wire it up using native html attributes so it opens and closes without any custom javascript event listeners. include a cancel button inside the dialog to dismiss it. also make the 'view details' button on the maple oat latte card toggle a popover with nutritional info completely without javascript. Additionally, for the Dark Roast Espresso card, add a button to explicitly show a popover and another button inside that popover to explicitly hide it.
