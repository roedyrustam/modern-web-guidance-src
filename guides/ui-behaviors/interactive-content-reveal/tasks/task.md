---
base_app: daily-grind
---
- add an interactive spotlight reveal effect to the seasonal favorites cards. first, give each seasonal card the 'product-card' class. then, create a non-interactive overlay element inside each card that uses a mask to reveal details within a circular spotlight area that follows the user's pointer. we want to use the CSS custom properties --mouse-x and --mouse-y on the card to track the position of the pointer relative to the card, and use --inner-size and --outer-size on the card to transition the spotlight size from 0px when not hovered to a non-zero size when hovered. make sure the size transition has a non-zero duration by default, but happens immediately (zero duration) for users who prefer reduced motion, while the position variables do not transition. the original text and buttons in the card must remain fully visible and clickable underneath.
- can you implement a dynamic spotlight pointer effect on our seasonal coffee product cards? make it so a circular mask follows the cursor over any .product-card using custom properties like --mouse-x and --mouse-y to track coordinates, while smoothly expanding the visible radius when hovered using --inner-size and --outer-size. ensure the base text is never hidden and stays keyboard accessible.
