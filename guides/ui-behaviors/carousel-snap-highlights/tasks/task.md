---
base_app: daily-grind
---
- please convert the seasonal favorites section into a horizontal scroll-snapping carousel. use a .carousel container and wrap each coffee card inside a .carousel-item div. the container should have auto horizontal overflow so users can swipe. by default, the .card elements should have a grey background #e0e0e0. when a card is currently snapped/centered, i want it to stand out by scaling up to 1.15, changing its background color to the blue #007bff, and displaying a shadow. ensure that if a user has reduced motion enabled in their system, we completely disable the scaling and color transitions so it stays static and safe.
- can you make the seasonal favorites coffee cards into a swipable horizontal row that automatically highlights and scales up the active card in the center as you scroll through them, while keeping the rest grey?
- implement a horizontal carousel for the seasonal favorites section with snapping. the active card in the middle of the viewport should scale to 1.15 and turn blue, but make sure to disable this animation entirely for reduced motion preferences.
