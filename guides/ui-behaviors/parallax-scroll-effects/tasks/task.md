---
base_app: daily-grind
---
- Add a scroll-driven parallax effect to a new section below the hero header. I want multiple layers to move at different speeds as the user scrolls, creating a sense of depth. Avoid using JavaScript for this effect and ensure it is accessible and performant.
- change the css for the parallax layers so it automatically staggers their scroll speeds.
- tweak the fallback script so it doesn't run if the browser already supports the native css animations.