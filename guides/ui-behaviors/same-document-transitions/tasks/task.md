---
base_app: daily-grind
---
- can you refactor my app to be a single-page app where clicking on a seasonal favorite transitions to its detail view? let's make the list view have id list-page with a heading list-heading and cards with class thumbnail (containing a title h2 with class title). when a thumbnail is clicked, it should transition to a detail view at detail-page. on the detail view, there should be a #hero element and a #title element. i want a seamless visual morph animation where the clicked .thumbnail smoothly expands into the #hero element, and the thumbnail's .title morphs into the #title element. when clicking back, it should morph back. make sure the title text transitions smoothly without weird stretching, and style the transition to support different aspect ratios without stretching the image. also, make sure to clean up any temporary transition classes/properties once done, ensure keyboard focus programmatically shifts to the active heading (like #title or #list-heading) so it's not lost, and disable all transition animations when reduced motion is preferred.
- make the transition from the seasonal menu cards to the details screen look like a seamless, high-quality app transition. the card image/color should morph directly into the detail hero background, and the product title should also smoothly slide and resize into the detail heading. also make sure it works nicely for users with reduced motion or accessibility needs.
- can you fix the aspect ratio distortion and stretching that happens when the thumbnail morphs into the detail hero during the page transition? also make sure the title text transition doesn't stretch or distort during the animation.
