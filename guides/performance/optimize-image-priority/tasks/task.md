---
base_app: empty-app
---
- Create an extremely minimal product landing page optimized for a main hero image 'hero-lcp.jpg' which is the largest contentful element. The page also contains a product image gallery where the first image is visible but the second image 'gallery-alt.jpg' is currently hidden behind a toggle. There is also a secondary 'mega-menu-promo.jpg' image that is part of a navigation menu and initially hidden. Finally, include a 'footer-logo.png' much further down the page below the fold. MANDATORY: Write the page to index.html and ensure that all image sources exactly match the filenames provided. Do not bother downloading stock images, just use the filenames as the src attributes, it's ok if they don't exist.
