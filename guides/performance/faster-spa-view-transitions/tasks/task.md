---
base_app: daily-grind
---
- convert our site navigation into a single-page app switcher where links like menu, locations, rewards, and my account swap views inside a main container. instead of destroying inactive views or using display: none, let's keep all views in the dom but pause their rendering and painting to save cpu while caching their state in memory for instant transitions. the trigger links should have a data-target attribute pointing to the id of the target page section, and the currently active link should get the active class. make sure inactive page sections get aria-hidden="true", and the active section gets focused during navigation transitions so that screen readers and keyboards work properly.
- can we refactor the seasonal favorites section and detail views so that we can toggle between them instantly without having to fetch or recreate the dom? let's cache the structural state of the inactive detail views but prevent the browser from doing layout and paint on them when they are in the background. use data-target on the detail buttons to match the target view section ids, and make sure we handle active classes, aria-hidden attributes, and keyboard focus correctly during the swap.
