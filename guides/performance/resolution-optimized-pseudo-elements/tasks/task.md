---
base_app: daily-grind
---
- add a decorative coffee bean icon right before each of the h3 titles under seasonal favorites. Use the images `coffee-bean.png` and `coffee-bean-2x.png` so the icon looks crisp on high-resolution retina displays without inserting extra HTML DOM elements or cluttering markup.
