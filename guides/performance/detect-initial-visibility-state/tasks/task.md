---
base_app: daily-grind
---
- can you add a script to our coffee shop homepage that accurately detects whether the page was initially loaded in a background tab and records the exact timestamp of when it was backgrounded? please log this detection info as a console object with the background status (boolean) and background time (milliseconds). since legacy fallback queries are highly inaccurate for async scripts, make sure to clearly flag or comment when the fallback detection is active and potentially unreliable.

