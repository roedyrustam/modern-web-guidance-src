---
name: combobox
description: "Build a combobox: a text input combined with a filterable popup list of options the user can type into to narrow and select from."
web-feature-ids:
  - customizable-select
  - datalist
---

<!-- Link to filterable-select if only filtering is needed -->
