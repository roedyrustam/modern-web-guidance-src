---
base_app: daily-grind
---
- Build a scroll-position-aware element for the floating "back to top" button with class "back-to-top" inside the "scroller" container. The button should start out hidden at the top of the container and smoothly animate into view as the user scrolls down, without using heavy continuous scroll event listeners or breaking keyboard tab navigation when hidden.
