---
base_app: daily-grind
---
- add a visual indicator at the top of the page that shows the user how far they have scrolled through the content. The indicator should fill up horizontally from left to right as the scroll progresses. Ensure it stays visible while scrolling. Style it using the `--accent` color. This effect must be implemented with progressive enhancement in mind, ensuring the indicator works smoothly in all browsers.
- can you build a reading progress indicator at the very top of the screen? make it grow from left to right based on how far down the user scrolls.
- implement a visual affordance to show how far down the page someone has scrolled. just a thin line anchored to the top is fine.