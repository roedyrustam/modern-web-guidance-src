---
base_app: daily-grind
---
- can you add an options dropdown menu to our coffee shop navigation bar? we need a trigger button with id trigger-btn and an overlay panel with id action-panel containing action buttons like 'seasonal offers', 'account settings', and 'support'. the panel needs to render in the browser's top layer to avoid z-index or parent overflow clipping issues, and must anchor directly below the trigger button, aligned to its left edge. make sure the panel's positioning is resilient, automatically flipping vertically or horizontally if it collides with the viewport boundaries so it is never cut off. also, make sure to avoid using aria-haspopup, role="menu", or role="menuitem" because we are not implementing arrow-key keyboard navigation for this panel.
- let's build a floating action list for the coffee shop's navigation bar that is anchored to a new button with id trigger-btn. the list should have id action-panel and should stay positioned directly below the button, but if it gets too close to the bottom of the screen, it should flip to show above the button to stay on-screen.
- please implement an interactive options overlay that is attached to a trigger-btn in the header of our coffee shop page. the overlay should use action-panel as its id, rendering above all other page content to avoid layout clipping, and must adjust its position automatically so that it never overflows the left, right, top, or bottom edges of the screen.
