---
base_app: daily-grind
---
- can you add a .resizable-container wrapping a .card, and make sure the card's styles respond directly to the width of that container? when the container is under 400px wide, the card should stack in a vertical column, but when its container width crosses 400px, it should shift to a horizontal row layout.
- i want to make the seasonal favorites coffee cards modular. can you build a responsive card component that modifies its own layout based on the width of the parent box it is placed in instead of the main viewport? when its container is squeezed below 400px wide, let's keep it stacked vertically, but when the container expands, change it to a side-by-side layout.
