---
base_app: daily-grind
---
- can you make the seasonal favorite coffee cards on the homepage scale their titles based on how wide each card is instead of the viewport width? please wrap each of the seasonal cards in a div with the class `card-wrapper` and add the class `card-title` to the card's `h3` heading element. make the text size scale smoothly relative to the width of its parent card, but keep the size constrained between a minimum of 1rem and a maximum of 2.2rem.
- could you adjust the seasonal favorites card titles so they size themselves dynamically? they look a bit weird when the layout shifts, so they should resize fluidly depending on how wide their individual card wrapper is. please make sure the wrapper has the class `card-wrapper` and the title heading has the class `card-title` so the styling is scoped correctly. the font size should scale nicely with the container width but remain clamped within a minimum and maximum readable range.
