---
base_app: daily-grind
---
- add a toggleable faq/accordion section for 'Our Sourcing' below the seasonal favorites in our home page. the accordion should have a button with id 'accordion-toggle' and content container with class 'accordion-content'. when expanded, the content container's block-size should smoothly animate from 0 to its natural height plus an extra 2rem of breathing room. let's also enable smooth transitions to and from height: auto globally across the site for simple toggles. make sure to use logical properties like block-size or inline-size for layout dimensions instead of physical width/height. also, add a styled badge with class 'promo-badge' inside the 'Maple Oat Latte' card that displays 'Staff Pick' and has its inline-size set to a proportion of its maximum content size—specifically 1.2 times the max-content size—while clamping it between 100px and 100% of the parent width. make sure the accordion animation is disabled or simplified for users who prefer reduced motion. lastly, include a CSS comment in the style block explaining that directly transitioning between different intrinsic keyword sizes isn't supported.
- can you make the seasonal favorites cards expand slightly in width when hovered over, but ensure they don't break the layout or get too wide? i want the sizing of each card to be dynamically calculated based on their contents but with some extra padding, and the transition should be smooth unless the user has motion restrictions.
- please build a sliding details panel for our menu items that expands its inline-size from 0 to 1.3 times the maximum content width on click. make sure it uses logical sizing and includes a prefers-reduced-motion media query to handle vestibular preferences.
