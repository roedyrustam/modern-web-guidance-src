---
base_app: daily-grind
---
- add a cool linear gradient background to the body and all h2 headings. also, make the main section responsive: on screens around 400px wide it should display a single column, but on larger screens around 1000px wide it should show two columns of equal width. let's keep the styles dry by using reusable custom function logic in the stylesheet to generate these gradients and columns instead of repeating ourselves.
- can you modernize the layout of our coffee shop site? make the body background and the h2 headings use gradient designs, and make the main container layout responsive so it's a single column on mobile and two columns of equal width on desktop. reuse your css rules and functions to keep it clean and avoid style repetition.
