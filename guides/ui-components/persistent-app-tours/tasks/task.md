---
base_app: daily-grind
---
- add an onboarding tour step for the rewards link in the top nav. Give the rewards link the ID `feature` and the tour step element the ID `tour-step`. MANDATORY: The tour step must visually track and stay positioned relative to the rewards link automatically. It must be open by default and remain open even if the user clicks other parts of the page, until they explicitly close it. Include a short message about earning points and a button that closes it. Ensure that clicking the rewards link during the tour does not cause the page to navigate away.
- can you build a persistent tooltip pointing to the 'order now' button in the hero section? Give the 'order now' button the ID `feature` and the tooltip element the ID `tour-step`. The tooltip must be open by default when the page loads. It should say 'skip the line, order ahead'. MANDATORY: It must stay reliably positioned near the button and must absolutely stay on screen until explicitly dismissed with its close button, ignoring clicks outside of it.
- we need a new feature callout next to the seasonal favorites grid. Give the maple oat latte card the ID `feature` and the callout popover the ID `tour-step`. MANDATORY: The callout must automatically tether its position to the card. It must be open by default when the page loads, and it must not disappear if I click on the background or interact with other elements. Make sure to include a 'got it' button to hide it.
