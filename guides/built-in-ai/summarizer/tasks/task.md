---
base_app: daily-grind
---
- add a "Roaster's Journal" section above the footer with some long-form text about our bean sourcing process. then add a "summarize" button that uses the browser's native ai to turn that text into a few bullet points right on the page.
- can you add a "tl;dr" button to each of the seasonal favorites cards? it should use the built-in summarizer to show a one-sentence teaser of the description.
- i want a way for users to get a quick recap of "our story" without leaving the homepage. add a button near the footer link that uses the local ai to generate a summary of what we're about.
