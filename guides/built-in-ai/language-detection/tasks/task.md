---
base_app: daily-grind
---
- can you add a feedback section at the bottom of the main container? it needs a textarea for the message and a button that says 'Detect'. when the user clicks the button, use the browser's built-in language detection to show the name of the language and how confident the AI is. also, make sure to check if the feature is available first and show a progress percentage if the model needs to be downloaded.
- hey, can you add a simple language identifier tool to the bottom of the page? i want people to be able to type some text and see what language it is using the new on-device ai features. it would be cool if it showed how sure it is too.
- could you add a box below the seasonal favorites where i can paste text and have it tell me the language? use the native browser capabilities for this and make sure it handles any model downloads by showing the progress.
