---
base_app: daily-grind
---
- can you add an interactive coffee order customizer right below the seasonal favorites? i want a canvas element with id 'canvas' that visually displays a coffee cup, but inside it, we should render actual interactive HTML inputs and text (like a name input and options for milk and syrup) so that users can select, type, and click. make sure that this canvas content is fully accessible, searchable via find-in-page, and translatable by the browser, and ensure that the interactive HTML layers align exactly with where they are drawn on the canvas. also, make sure the canvas is scaled properly to prevent any blurriness on high-resolution displays.
- can you make sure that whenever the user changes their choices on the customizer, the visual elements on the canvas update instantly so it feels like a real-time reactive app
- could you style the coffee cup and the form controls inside the canvas to match our dark brown and warm cream aesthetic
