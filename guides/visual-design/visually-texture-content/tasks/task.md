---
base_app: daily-grind
---
- can you add a weathered, rustic grunge effect to the main hero section? please apply the class name `weathered-bg` to the hero element, and use the `grunge-texture.svg` file as a repeating mask so that the distress pattern physically eats into the background transparency.
- let's make the design feel a lot more rustic and physical by applying a subtle distressed texture overlay on the main hero banner so it looks like printed ink on a raw paper bag.
- could you update the hero area to have a physical organic distress pattern using `grunge-texture.svg`? make sure the hero element gets the class `weathered-bg` so it matches our design specs.
