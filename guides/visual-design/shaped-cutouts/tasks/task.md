---
base_app: daily-grind
---
- can you add some new shaped elements to the page? we need: 1) a featured barista profile image with class .shaped-avatar that is cropped using an organic paint splat vector shape that scales correctly with the image's dimensions, 2) a loyalty banner with class .unified-card that contains an unmasked text block with class .card-body and an adjacent empty decorative element with class .card-accent which has a custom cutout curve mask on its side, made to look like a single card with a nice shadow filter on .unified-card, and 3) a promo card with class .gradient-masked-card that has a circular notch cutout on its top edge using a gradient.
- can you make the coffee shop homepage's cards and images look less blocky by adding curved notch cutouts and organic shapes, but keep all the text inside readable and clear without getting cropped?
- add a coupon card at the bottom of the homepage using class .gradient-masked-card that looks like a ticket with a semi-circle cutout on its side edge using a css mask so it stands out to customers.
