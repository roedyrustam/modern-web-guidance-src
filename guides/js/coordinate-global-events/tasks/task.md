---
base_app: daily-grind
---
- let's add a global virtual coffee tasting event scheduler to our cafe website, styled to match the existing dark brown color palette. create a section under our seasonal favorites with the heading "Global Tasting Coordinator" and a description of the tool. inside, build a timezone-aware planner with a date input #dateInput, a time input #timeInput, and a host timezone select dropdown #hostTzSelect (containing option values like America/New_York, Europe/London, Asia/Tokyo, and UTC). when a user changes these, calculate the equivalent times for New York, London, Tokyo, and UTC, and display them in a grid with id #grid where each timezone has its own card showing the formatted local time (e.g. "3:00 AM"). make sure the calculations use modern, native timezone-aware javascript date-time objects (not the legacy Date object) and its built-in timezone conversion methods to compute target times safely. additionally, if the chosen time is ambiguous or does not exist due to a daylight saving time transition gap or overlap, detect this conflict and show a clear explanation in an element with id #hostWarning describing how the time was resolved, ensuring the underlying time-zone-aware object is treated as immutable.
- add a timezone-safe meeting planner under our main seasonal favorites container so that our international partners can coordinate wholesale coffee bean orders and tasting sessions. it needs to handle clocks shifting for dst properly without scheduling mistakes.
